let countHome = document.getElementById("home-disp");

function Home(count) {
    countHome.textContent = parseInt(countHome.textContent) + count;
}

let countGuest = document.getElementById("guest-disp");

function Guest(count) {
    countGuest.textContent = parseInt(countGuest.textContent) + count;
}

